const colors = require("../colors/default");

const config = {
  name: "Owlet (Palenight)",
  type: "dark",
  fontStyle: ""
};

const shades = {
  background: "#292d3e"
};

module.exports = {
  config,
  shades,
  colors
};

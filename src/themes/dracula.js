const colors = require("../colors/default");

const config = {
  name: "Owlet (Dracula)",
  type: "dark"
};

const shades = {
  background: "#282a36"
};

module.exports = {
  config,
  shades,
  colors
};

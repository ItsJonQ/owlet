const colors = require("../colors/default");

const config = {
  name: "Owlet (Charcoal)",
  type: "dark"
};

const shades = {
  background: "#101010"
};

module.exports = {
  config,
  shades,
  colors
};

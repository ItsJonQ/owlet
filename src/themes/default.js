const colors = require("../colors/default");

const config = {
  name: "Owlet (Default)",
  type: "dark"
};

const shades = {
  background: "#1e222a"
};

module.exports = {
  config,
  shades,
  colors
};

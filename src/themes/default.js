const colors = require("../colors/default");

const config = {
  name: "Owlet (Default)",
  type: "dark",
  fontStyle: ""
};

const shades = {
  background: "#1e222a",
  text: "#737e83"
};

module.exports = {
  config,
  shades,
  colors
};

const colors = require("../colors/default");

const config = {
  name: "Owlet (Purple)",
  type: "dark",
  fontStyle: ""
};

const shades = {
  background: "#27234b",
  text: "#897f93"
};

module.exports = {
  config,
  shades,
  colors
};

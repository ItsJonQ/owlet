const colors = require("../colors/default");

const config = {
  name: "Owlet (Night Owl)",
  type: "dark"
};

const shades = {
  background: "#011627"
};

module.exports = {
  config,
  shades,
  colors
};
